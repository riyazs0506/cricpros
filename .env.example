# Example environment variables for local development
# Copy this file to `.env` and fill in real values. Do NOT commit your `.env` file.

# A secret key used by Flask sessions and other cryptographic components
SECRET_KEY=replace-me-with-a-secure-random-string

# SQLAlchemy connection string example (replace user/password/host/port/dbname)
# For example: mysql+pymysql://user:password@host:3306/database_name
DATABASE_URL=mysql+pymysql://user:password@host:port/dbname
